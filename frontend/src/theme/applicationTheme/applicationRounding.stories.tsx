import { Meta, StoryObj } from "@storybook/react";
import { RoundingElements } from "src/_test_utilities/SpacingAndRoundingElements";

const meta: Meta = {
  title: "Style/Rounding",
  tags: ["autodocs"],
  component: RoundingElements,
  argTypes: {},
};

export default meta;

type Story = StoryObj;

export const RoundingStyles: Story = {};
