/**
 * Enum for identifying all the lambda functions in the backend
 */
export enum Lambdas {
  AUTH = "AUTH",
  API = "API",
  IMPORT = "IMPORT",
  EXPORT = "EXPORT",
  EMBEDDING = "EMBEDDING",
  PUBLISH_EMBEDDINGS_TASK = "PUBLISH_EMBEDDINGS_TASK",
}
