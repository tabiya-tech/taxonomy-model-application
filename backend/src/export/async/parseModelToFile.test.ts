import { parseModelToFile } from "./parseModelToFile";

describe("Test the parseModelToFiles handler", () => {
  test.todo("It should parse the model to files and zip");
  // should update the export process state to RUNNING
  // should stream data from the model -> csv-stringifier -> archiver -> s3 without any errors
  // should do this in a reasonable amount of time and with reasonable memory usage
  test.todo("Should throw errors when they occur");
});
