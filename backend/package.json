{
  "name": "backend",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf build coverage deploy",
    "build": "tsc --build tsconfig.prod.json && cp ./package.json ./build/  && (cd ./build; npm pkg delete devDependencies; npm pkg delete scripts)",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "@types/aws-lambda": "~8.10.114",
    "@types/jest": "~29.5.0",
    "jest": "~29.5.0",
    "ts-jest": "~29.1.0",
    "typescript": "~4.4.2"
  },
  "dependencies": {

  }
}
