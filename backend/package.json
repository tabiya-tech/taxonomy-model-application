{
  "name": "frontend",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf build",
    "build": "tsc && cp ./package.json ./build/"
  },
  "devDependencies": {
    "@types/aws-lambda": "~8.10.114",
    "@types/jest": "~29.5.0",
    "jest": "~29.5.0",
    "ts-jest": "~29.1.0"
  },
  "dependencies": {
    "typescript": "~4.4.2"
  }
}
